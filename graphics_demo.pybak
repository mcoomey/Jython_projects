def main():
    pic = makeEmptyPicture(800,600)
    
    for x in range(0,800,50):
        pic.addLine(blue, x, 0, x, 600)

    
    for y in range(0,600, 50):
        pic.addLine(red, 0, y, 800, y)
        
        
    show(pic)
